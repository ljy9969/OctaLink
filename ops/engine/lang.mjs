// 언어 가드: 로컬 오픈소스 모델(qwen 등)이 한국어 대신 중국어/영어로 흐르는 것을 막는다.
// CEO·에이전트 시스템 프롬프트 앞에 항상 붙인다(검증기는 JSON만 내므로 제외).
export const LANG_GUARD =
  '[출력 언어] 모든 응답은 반드시 한국어로만 작성한다. 중국어·영어 등 다른 언어로 쓰지 말 것(제품명·코드·URL 등 고유값은 예외).\n\n';
